language: node_js

node_js:
  - 0.12
  - 5.0.0

env:
  - XUNIT_FILE=shippable/testresults/result.xml

build:
  ci:
    - npm install
    - npm test
    - npm test1
integrations:
  notifications:
    - integrationName: Myslack
      type: slack
      recipients:
        - "#mybuilds"
      on_start: always
      on_success: always
