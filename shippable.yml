language: node_js

node_js:
  - 0.12

env:
  - XUNIT_FILE=shippable/testresults/result.xml

build:
  ci:
    - npm install
    - npm test
    
integrations:
  notifications:
    - integrationName: slack-macha
      type: slack
      recipients:
        - "#niraj93"
      on_start: always
      on_success: always
      on_failure: always
