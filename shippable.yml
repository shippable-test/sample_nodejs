language: node_js

node_js:
  - 0.12

env:
  - XUNIT_FILE=shippable/testresults/result.xml

build:
  ci:
    - npm install
    - npm test
    
integrations:
  - integrationName: Myslackq
    type: notification
    recipients:
      - "#mybuilds"
    on_start: always
    on_success: always
